-- Layout Configuration
-- Standalone CachyOS Hyprland

-- Dwindle Layout

hl.config({

	dwindle = {

		-- Preserve Split

		preserve_split = true,
	},
})

-- Master Layout

hl.config({

	master = {

		-- New Window Behaviour

		new_status = "master",
	},
})

-- Scrolling Layout

hl.config({

	scrolling = {

		-- Fullscreen

		fullscreen_on_one_column = true,
	},
})
